// create a object and inside that  object take a function, which should add two number and return them.

let form= {
  
  sum : function (a,b){
    return a+b;
  }
}

let output= form.sum(1,2);

console.log(output);